apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: nestjs-backend-route
spec:
  to:
    kind: Service
    name: nestjs-backend-service
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
